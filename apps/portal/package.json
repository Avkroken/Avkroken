{
  "name": "avkroken-portal",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --check public/app.js && node --check public/shell.js && node --check public/docs.js && node --check public/wiki.js && node --check public/search.js && node --check public/operations.js && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.69.0"
  }
}
